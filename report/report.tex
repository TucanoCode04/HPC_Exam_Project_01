\documentclass[11pt,a4paper]{article}
\title{HPC Project Report}
\author{Leonardo Antonio Riola, Davide Donati, Giacomo Girotto}
\date{}
\begin{document}
\maketitle
\section{Introduction (??)}
\section{Assignment 1}
\subsection{Introduction and objectives}
The objective of this project is to implement a simulator that models the propagation of a damped wave. The general partial differential equation describing the phenomenon in space as time varies is the following:
\begin{equation}
    \frac{\partial^{2}u}{\partial t^{2}}+\gamma\frac{\partial u}{\partial t}=c^{2}\nabla^{2}u
\end{equation}
Where $u(x,y,t)$ represents the height of the wave on a two-dimensional plane over time, $\gamma$ is the wave damping coefficient, and $c$ is the wave propagation speed. The simulator calculates the wave height for each point in the coordinate space $(x, y)$ as time $t$ changes, with the ultimate goal of generating an MP4 video of the simulation.


\subsection{Implementation}
To perform the simulation, it is necessary to convert the continuous model into a discrete model suitable for implementation on a computer. For this purpose, a grid is introduced: space is divided into equally spaced points along the x and y directions with steps $\Delta x$ and $\Delta y$, and time is discretised into instants separated by a time step $\Delta t$.
Consequently, each point in space is identified by integer indices $(i,j)$, and time by an index $t$. The continuous function $u(x,y,t)$ is replaced by a matrix of values $u_{i,j}^{t}$, representing the value of the function at the grid point $(i,j)$ at time step $t$. The derivatives appearing in the differential equation are approximated using finite differences. In particular, both central and forward difference methods are implemented in the code and can be tested separately.


To handle the temporal evolution of the wave, three distinct $M \times M$ matrices are dynamically allocated in memory using the \texttt{malloc} function in C. These matrices, defined as \texttt{u\_past}, \texttt{u}, and \texttt{u\_fut}, represent the wave state at the previous time step ($t-1$), the current time step ($t$), and the future time step ($t+1$), respectively. Initially, all matrix elements are set to zero, and an initial impulse in a specific locationis assigned to start the perturbation.

To accelerate the computation, the program utilizes the \textbf{OpenMP} library as parallelization framework to generate the matrices representing the evolution of the wave, which is also the core of this assignment. Instead of calculating the new state of the grid sequentially, the workload is distributed across multiple active threads: this is performed using the \texttt{\#pragma omp parallel} directive within the code just before the loop for the temporal advancement. Then, by parallelizing the nested spatial loops using the \texttt{\#pragma omp for schedule(static) collapse(2)} directive, the computation of individual matrix elements is executed concurrently. The \texttt{schedule(static)} clause dictates that the loop iterations are divided into chunks of equal size and assigned to the threads before execution begins. Since the mathematical calculation for each matrix element is uniform, a static schedule eliminates the runtime overhead associated with dynamic task allocation. Furthermore, the \texttt{collapse(2)} clause is utilized to fuse the two nested spatial loops (iterating over rows $i$ and columns $j$) into a single iteration space of size $M \times M$. This significantly increases the total number of independent iterations available for distribution, ensuring a finer and more balanced division of the workload among the active cores.
By doing so, while one thread computes a future value $u_{i,j}^{t+1}$ for a specific coordinate utilizing the current and past values ($u_{i,j}^{t}$ and $u_{i,j}^{t-1}$), all the other threads are simultaneously computing the future values for different coordinates.
This operation won't generate any race conditions, as to each thread are assigned different elements of the \texttt{u\_fut} matrix.

Once the entire \texttt{u\_fut} matrix has been fully calculated for the current temporal iteration, the program must write the costructed matrix to a Portable Graymap (PGM) file format. To do so, the program uses a \texttt{\#pragma omp single} directive to ensure that only one thread performs the writing operation, while the other threads will wait at an implicit synchronization barrier.
Finally, the simulation must advance to the next time step. To maximize efficiency and eliminate the computational overhead of deep-copying large memory blocks, time progression is achieved through a cyclic pointer swap. This operation is executed by the same thread which performed the writing to the PGM file, inside the \texttt{\#pragma omp single} construct. This guarantees that only one designated thread performs the pointer exchange (\texttt{u\_past} takes the address of \texttt{u}, \texttt{u} points to \texttt{u\_fut}, and \texttt{u\_fut} is recycled to store the next iteration), preventing race conditions and strictly preserves memory efficiency before the threads begin computing the next time step.


\subsection{Test}














\section{Assignment 2}
\input{../Giro_part/Esercizio_2/sezione_giro_2}
\section{Assignment 3}
\end{document}